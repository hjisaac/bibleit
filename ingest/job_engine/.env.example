# Copy this file to ingest/.env (not job_engine/.env -- dotenv loading
# is CWD-based, and jobs are run from ingest/) and fill with real values.
# Never commit .env with real secrets.

# Weights & Biases API key for experiment tracking (see WBTracker).
WANDB_API_KEY=your_wandb_api_key_here
